class Request {
  String posterId;
  String requesterId;
  bool status;
  String postId;
  String requestId;
  Request(this.postId, this.posterId, this.requesterId, this.status,
      this.requestId);
}
